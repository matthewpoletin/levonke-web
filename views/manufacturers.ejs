<!DOCTYPE html>
<html>
    <head>
        <%- include('head', {}); %>
    </head>
    <body>
        <%- include('header', {}); %>
        <div class="content">
            <% if (manufacturers) { %>
            <ul>
                <% manufacturers.forEach(function(manufacturer) { %>
                <li>
                    Name: <a href="/manufacturer/<%= manufacturer.id %>"><%= manufacturer.name %></a><br>
                    <s>Description: <%= manufacturer.description %></s>
                </li>
                <% }); %>
            </ul>
            <% } %>
        </div>
        <%- include('footer', {}); %>
    </body>
</html>
